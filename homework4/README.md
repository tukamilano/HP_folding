Hint1: 偶奇による判定
スタンプの大きさが偶数の場合, ○の数-×の数の偶奇をスタンプによって変えることはできない. そのため, ○の数-×の数が異なる状態に遷移することは不可能である.

# 対称性を考えて <- canonicalizeする(よってBFSでは盤面をコピーする方式を取る, つまりmove, undoするみたいな方法は取らない)
# bitboard化することに決めた! <- ボードの大きさはハードコードする
# Boardインスタンスは使わずbitarrayで管理(コピーの重さを考慮する)
# 幅優先探索と深さ優先探索ではA*で使うようなヒューリスティクス関数を採用(1の数の少なさ)


